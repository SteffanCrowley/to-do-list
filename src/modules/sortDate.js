import format from "date-fns/format";
import parseISO from "date-fns/parseISO";

let currentArray = [];

function storeList(myList) {}

function sortWeek() {
  console.log("x");
}

export { storeList, sortWeek };
